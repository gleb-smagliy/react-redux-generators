import { connect } from 'react-redux';
<% if(hasMapDispatch) { %>
import { bindActionCreators } from \'redux\';'
<%}%>
import selectors from './selectors';
import actions from './actions';
import moduleId from './constants/module-id';

<% if(hasMapDispatch) { %>
export const mapDispatch = dispatch =>
{
  return {

  };
};
<%}%>

<% if(hasMapProps) { %>
export const mapProps = state =>
{
  return {

  };
}
<%}%>

<% if(hasMapProps && hasMapDispatch) { %>
export default connect(mapProps, mapDispatch)(<div/>);
<%} else if(hasMapProps) {%>
export default connect(mapProps)(<div/>);
<%} else if(hasMapDispatch) {%>
export default connect(undefined, mapDispatch)(<div/>);
<%}%>

export { moduleId };
export { selectors };
export { actions };
export { default as actionTypes } from './action-types';
export { default as saga } from './saga';