import moduleId from '.../constants/module-id';

export default {
<%_ actions.forEach(function (action, i) { _%>
    <%= action.toUpperCase() %>: `${moduleId}/<%= action.toUpperCase() %>`<%= i < actions.length-1 ? ',' : '' %>
<%_ }) _%>
};