import React from 'react';
import PropTypes from 'prop-types';

const <%= pascalizedComponentName %> = ({
  <%_ include props _%>
}) => (
<div />
);

export const defaultProps = {};

export const propTypes = {
  <%_ include props _%>
};

<%= pascalizedComponentName %>.defaultProps = defaultProps;
<%= pascalizedComponentName %>.propTypes = propTypes;

export default <%= pascalizedComponentName %>;
