import moduleId from '../constants/module-id';
import selector from './<%= selector %>';

describe('<%= `${containerName} ${selector}` %> selector', () =>
{
  const moduleState = {

  };

  const state = {
    [moduleId]: moduleState
  };

  it(
    'Should select full state',
    () =>
    {
      expect(selector(state)).to.deep.equal(moduleState);
    }
  );
});
