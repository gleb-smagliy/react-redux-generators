import React from 'react';
import PropTypes from 'prop-types';

class ComponentName extends React.<% isPure ? "PureComponent" : "Component" %>
{
 static propTypes = {};

 static defaultProps = {};

 render()
 {
 return (<div className="-re-component-name">Component</div>);
 }
 }

export default ComponentName;